{
  "$schema": "https://schema.railpack.com",
  "steps": {
    "build": {
      "commands": [
        "npx prisma generate",
        "npm run build"
      ]
    }
  },
  "deploy": {
    "startCommand": "sleep 5 && npx prisma migrate deploy && npm run start:prod"
  }
}
